Place reference UI images here. Filenames used by the app:

- `hero.jpg` — hero image shown on the landing page (suggested resolution ~1600x900)
- `dashboard_preview.png` — dashboard preview used on landing/SDK panels (~1200x800)
- `sidebar_logo.png` — small logo for the sidebar (optional)

You can copy the images you attached into this folder with the above names.
